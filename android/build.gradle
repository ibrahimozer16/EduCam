buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 24
        compileSdkVersion = 35
        targetSdkVersion = 35
        ndkVersion = "26.1.10909125"  // Güncellendi!
        kotlinVersion = "2.0.21"
    }
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }  // Vision Camera için gerekli!
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.2.0")  // Güncellendi!
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
        classpath 'com.google.gms:google-services:4.4.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // 📌 Vision Camera'nın ihtiyacı olan repo
    }
}

apply plugin: "com.facebook.react.rootproject"
